FreeSimpleGUI
pyautogui
schedule
pywin32
# pywin32 (opcional, apenas Windows - melhora redimensionamento de janelas)
# Instale com: pip install pywin32

# Para criar executável:
# PyInstaller
# Instale com: pip install pyinstaller

